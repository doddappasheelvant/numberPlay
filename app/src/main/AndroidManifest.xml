<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dosta.numberpaly">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".num2_result" android:screenOrientation="sensorPortrait"></activity>
        <activity android:name=".num1" android:screenOrientation="sensorPortrait"></activity>
        <activity android:name=".num2" android:screenOrientation="sensorPortrait"></activity>
        <activity android:name=".num3" android:screenOrientation="sensorPortrait"></activity>
        <activity android:name=".GameType" android:screenOrientation="sensorPortrait" ></activity>
        <activity android:name=".MainActivity" android:screenOrientation="sensorPortrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>